{
  "name": "semantic-graph-democracy",
  "version": "0.5.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "SGD — collective participation via a semantic graph, on GitHub + GitHub Pages + WebLLM.",
  "scripts": {
    "validate": "node scripts/validate-submission.mjs",
    "process": "node scripts/process-graph.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
